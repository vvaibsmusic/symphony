# Deployment Rule
Automatically push all codebase changes and deployments to the remote repository (`origin` and `github`) when completing a task, unless explicitly told otherwise. You do not need to wait for my explicit permission to run `git push`.
